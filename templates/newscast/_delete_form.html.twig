<form method="post" action="{{ path('newscast_delete', {'id': newscast.id}) }}" onsubmit="return confirm('Attention : veux tu vraiment supprimer ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ newscast.id) }}">
    <button class="btn">Supprimer</button>
</form>
