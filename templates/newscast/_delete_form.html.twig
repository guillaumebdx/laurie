<form method="post" action="{{ path('newscast_delete', {'id': newscast.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ newscast.id) }}">
    <button class="btn">Delete</button>
</form>
