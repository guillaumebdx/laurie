{% extends 'base.html.twig' %}

{% block title %}Edit info{% endblock %}

{% block body %}
    <h1>Editer l'info</h1>
    <p>
        Dans la mesure du possible, utilise des images plutôt foncées pour que le texte blanc apparaisse proprement.
    </p>
    {{ include('newscast/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('newscast_index') }}">Retour à la liste</a>
{% endblock %}
