<form method="post" action="{{ path('social_delete', {'id': social.id}) }}" onsubmit="return confirm('Attention ! Veux tu vraiment supprimer ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ social.id) }}">
    <button class="btn">Supprimer</button>
</form>
